language: python
python:
    - "2.7"
env:
    - PGVERSION=9.1
    - CKANVERSION=master
    - CKANVERSION=2.4
    - CKANVERSION=2.5
install:
    - bash bin/travis-build.bash
services:
    - redis-server
script: sh bin/travis-run.sh
before_install:
    - pip install codecov
after_success:
    - codecov
